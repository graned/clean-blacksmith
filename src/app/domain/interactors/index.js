const dataLayerInteractor = require('./dataLayerInteractor');
const domainLayerInteractor = require('./domainLayerInteractor');
const viewLayerInteractor = require('./viewLayerInteractor');

module.exports = {
  dataLayerInteractor,
  domainLayerInteractor,
  viewLayerInteractor,
};
